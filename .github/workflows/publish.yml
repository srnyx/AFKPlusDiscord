name: Publish
on:
  release:
    types:
      - released
  workflow_dispatch:

jobs:
  build:
    uses: srnyx/tools/.github/workflows/publish.yml@main
    with:
      modrinth: afkplusdiscord
      curseforge: curseforgeId
      java: 11
      versions: ">=1.19.3"
    secrets: inherit
    permissions:
      contents: write
